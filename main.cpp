#include <iostream>
#include "./header/datastructures.h"
#include <forward_list>

int main() 
{
    // DS::Forward_List<int> lst = {9,9,1,2,3,4,9,9,9,5,6,7,8,9,9};
    // DS::Forward_List<int> lst2 = {9,10,11,12,13,14};
    // lst.sort();
    // lst.remove(9);
    // std::cout << lst.unique() << std::endl;

    // for (auto it = lst.begin(); it != lst.end(); ++it)
    //     std::cout << *it << " ";

    // std::cout << std::endl;
    

    // lst2.reverse();
    // for (auto it = lst2.begin(); it != lst2.end(); ++it)
    //     std::cout << *it << " ";

    // std::cout << std::endl;

    // std::forward_list<int> l = {1,2,1,1,1,3,4,5,1,6,7,8};
    // l.unique();
    // // l.remove(0);

    // for (auto i : l)
    //     std::cout << i << " ";

    // std::cout << std::endl;
    return 0;
}